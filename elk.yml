############### elk env #################

elk:
        connect:
                url : https://localhost:9200
                ssl : /etc/elasticsearch/certs/http_ca.crt   # disable : ''
                user : elastic
                pass : Welcome#1
        docmap:
                status : status_map.json
                global : global_map.json
